# Core ML Libraries
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Speech Processing
librosa>=0.10.0
soundfile>=0.12.0
noisereduce>=2.0.0
webrtcvad>=2.0.10

# Audio Processing
pydub>=0.25.1
scipy>=1.10.0
numpy>=1.24.0
pandas>=2.0.0

# ML Utilities
scikit-learn>=1.3.0
fastdtw>=0.3.4
python-speech-features>=0.6

# Hugging Face Transformers (for Whisper)
transformers>=4.30.0
datasets>=2.13.0
accelerate>=0.20.0

# For Montreal Forced Aligner (if we want to use it)
# Note: MFA requires additional setup with conda/Kaldi
# Alternatively, we can use Whisper's forced alignment or simpler methods

# Web Framework (already in backend, but listing for completeness)
# fastapi>=0.100.0
# uvicorn>=0.23.0
# python-multipart>=0.0.6

# Audio I/O
sounddevice>=0.4.0
